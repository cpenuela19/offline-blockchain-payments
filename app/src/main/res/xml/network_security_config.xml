<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permitir cleartext (HTTP) para desarrollo local -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador Android -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- localhost -->
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- IPs de red local (para dispositivos físicos) -->
        <!-- Agrega aquí la IP de tu máquina si es diferente -->
        <domain includeSubdomains="true">192.168.0.9</domain>
        <domain includeSubdomains="true">192.168.0.15</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
    
    <!-- Para producción, solo HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

